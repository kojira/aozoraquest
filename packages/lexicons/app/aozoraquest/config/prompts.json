{
  "lexicon": 1,
  "id": "app.aozoraquest.config.prompts",
  "defs": {
    "main": {
      "type": "record",
      "description": "System prompt for a local-LLM-backed feature (admin-owned, applied to Gemini Nano etc.)",
      "key": "any",
      "record": {
        "type": "object",
        "required": ["id", "body", "updatedAt"],
        "properties": {
          "id": { "type": "string", "knownValues": ["spiritChat", "draftPost", "advancedDiagnosis"] },
          "body": { "type": "string", "maxLength": 8000 },
          "notes": { "type": "string", "maxLength": 500 },
          "maxNewTokens": { "type": "integer", "minimum": 1, "maximum": 200, "description": "Generation token budget. UI uses this as a soft cap on output length (web LocalLLM abstraction enforces). If unset, UI falls back to a sane default per feature." },
          "updatedAt": { "type": "string", "format": "datetime" }
        }
      }
    }
  }
}
